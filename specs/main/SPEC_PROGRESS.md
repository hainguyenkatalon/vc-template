# SPEC_PROGRESS

## 2025-11-17
- Bootstrap complete: `specs/main` scaffold + helpers in place.
- Blocker: waiting on stakeholder-provided requirements so FEATURE work can begin.

## 2025-11-25
- Helper scripts refined: `gen-spec-context.sh` now uses the generic `<!-- @spec-context:start -->` marker to strip template preamble so generated `SPEC_CONTEXT.md` starts at the paths section; guides updated accordingly.
- Blocker persists: still awaiting product requirements to populate `SPEC.md`.
